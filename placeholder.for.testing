This file is a placeholder file in the sandbox branch, so there is an area where
things can be tried out without disturbing too much

New xmlrpc attempt with external poster
First change
Inserted line on concurrent change
Second change
Another inserted line on concurrent change
Third (this should conflit) change
Appending some stuff here, so i'm sure to get
a conflict on pushing this first and then 
pulling that into something else
