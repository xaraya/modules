<xar:template xmlns:xar="http://xaraya.com/2004/blocklayout">
<xar:comment>
    Initialise the jquery.minitabs tool.
</xar:comment>

<xar:comment>
    Make sure jQuery is going to be loaded.
</xar:comment>

<xar:template file="jquery" module="jquery" />

<xar:comment>
    Load the main plugin javascript.
</xar:comment>

<xar:base-include-javascript filename="jquery.minitabs.js" module="jquery" />
<xar:style file='jquery.minitabs' scope="module" module="jquery" />

<xar:comment>
    Set up a 'ready' function so PNGs are fixed automatically.
    By default this fixes all PNGs in the body, but can be narrowed down
    if there is a desire to not fix some PNGs.
</xar:comment>

<xar:if condition="empty($args)">
    <xar:set name="$args">""</xar:set>
</xar:if>

<xar:if condition="!empty($selector)">
    <xar:base-include-javascript type="code" code="jQuery(document).ready(function(){jQuery('$selector').minitabs($options);});" />
<xar:else />
    <xar:base-include-javascript type="code" code="jQuery(document).ready(function(){jQuery('div.tabwrapper').minitabs($options);});" />
</xar:if>
</xar:template>