function update_hours_remaining() {
    var prev_hours = getElementById('hours').value;
    var prev_hours_remaining = getElementById('hours_remaining').value;

    prev_hours = 
    getElementById('hours_remaining').value = getElementById('hours_remaining').value - getElementById('hours').value
    
}